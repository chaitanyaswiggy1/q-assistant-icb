# Streamlit Cloud secrets — paste these in App → Settings → Secrets
# Do NOT commit this file with real keys.

PORTKEY_API_KEY     = "pk-..."
PORTKEY_VIRTUAL_KEY = "anthropic-..."   # Virtual key created in app.portkey.ai → Virtual Keys
