# Streamlit Cloud secrets — App → Settings → Secrets
# Paste the contents below (without the .example extension) into Streamlit Cloud.
# Free key from: https://aistudio.google.com → "Get API Key"

GOOGLE_API_KEY = "AIza..."
